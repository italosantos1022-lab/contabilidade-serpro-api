version: '3.8'

services:
  contabil-api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
    networks:
      - contabil-network

networks:
  contabil-network:
    driver: bridge
